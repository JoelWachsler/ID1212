// @ts-check

/**
 * Defines a point with an x and y coordinate.
 */
export default class Point {
  /**
   * @param {number} x 
   * @param {number} y 
   * @api public
   */
  constructor(x, y) {
    this.x = x;
    this.y = y;
  }
}
